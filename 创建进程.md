# 创建进程

```c++
struct task_struct {
  long state;
  struct mm_struct *mm;
  pid_t pid;
  struct task_struct__rcu *parent;
  struct list_head children;
  struct fs_struct *fs;
  struct files_struct *file;
}
```